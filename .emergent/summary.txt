<analysis>**original_problem_statement:**
The user wants to build a web app called Onboarding-Automat (Onboarding Automaton), a Micro-SaaS to be installed on their own server.

**PRODUCT REQUIREMENTS:**
- **Goal:** Standardize onboarding/offboarding in SMEs with clear responsibilities, automatic deadlines relative to a start date, transparent tracking, and an auditable final report.
- **MVP Scope:**
    - Role-based templates which generate onboarding cases with tasks.
    - Comments per task.
    - Optional email reminders.
    - Printable/PDF-capable report/proof of completion.
- **Core Logic:**
    - **Templates** for roles consist of **TemplateTasks**.
    - A **TemplateTask** has a category (IT, Admin), owner role (IT, Office), and an offset in days from the start date.
    - Creating a **Case** generates tasks from a template, resolves owner roles to specific emails, and calculates due dates.
    - Changing a case's start date reschedules open tasks.
- **Roles:** Admin, Manager, Owner, Read-only/Auditor.
- **Data Model:** Includes , , , , , , , .
- **UX/Screens:** Login, Dashboard, Case List, New Onboarding Wizard, Case Detail, Template Editor, Settings, Report.
- **Phase 2 / Future:** Role Change flow, Offboarding, Evidence Upload, Slack/Jira integration, SSO.

The user also made the following choices:
- Auth: JWT Token
- PDF Generation: Server-side
- Design Style: Modern/Corporate
- Language: German (Deutsch)

**User's preferred language**: Deutsch (German). The next agent MUST respond in German.

**what currently exists?**
A functional full-stack application for managing employee onboarding and offboarding processes. The backend is built with FastAPI and MongoDB, and the frontend with React and Shadcn UI.

The application supports:
- User authentication (JWT).
- Role-based access control (Admin, Manager, Owner).
- Creating and managing templates for different roles (e.g., Developer, Sales).
- Creating Onboarding and Offboarding Cases from these templates.
- Tracking task completion within each case.
- An evidence upload feature for tasks.
- A full audit log for tracking all significant actions.
- GDPR compliance features, including a privacy center for data export and deletion requests.
- The entire UI and all functionalities are implemented in German.

**Last working item**:
- **Last item agent was working:** The agent was tasked with implementing two new features:
    1. A cron job for automatic data cleanup to comply with GDPR data retention policies.
    2. A Role Change flow, a feature planned for Phase 2 to manage tasks for employees switching roles internally.
    The agent acknowledged the request and started by viewing the  file to understand the existing structure before implementation. No code changes have been made for these features yet.
- **Status:** NOT STARTED
- **Agent Testing Done:** N
- **Which testing method agent to use?** both
- **User Testing Done:** N

**All Pending/In progress Issue list**:
There are no pending issues. The current work is focused on new feature development.

**In progress Task List**:

- **Task 1: Cron-Job für automatische Datenbereinigung einrichten (P0)**
    - **Where to resume**: A mechanism for running background jobs needs to be created. This could be a separate Python script triggered by a system cron job. The script should contain the logic to connect to the database and delete old, completed cases based on a defined retention period (e.g., > 1 year old). This logic needs to be added to the backend.
    - **What will be achieved with this?** This will automate GDPR compliance regarding data retention (Speicherbegrenzung) by periodically cleaning up old personal data.
    - **Status:** NOT STARTED
    - **Should Test frontend/backend/both after fix?** backend
    - **Blocked on something:** None

- **Task 2: Role Change Flow implementieren (P1)**
    - **Where to resume**: This requires significant backend and frontend work.
        - **Backend:** Update the data models in  to support a Role Change case type (e.g., add  to the  model). Create new API endpoints to handle the creation and management of these new cases. It might need its own set of templates.
        - **Frontend:** Create a new wizard/page similar to  for initiating a role change. Update the dashboard () and case list () to display and filter these new cases.
    - **What will be achieved with this?** This will extend the app's utility beyond just hiring and firing, allowing it to manage internal employee transitions, a key feature from the original roadmap.
    - **Status:** NOT STARTED
    - **Should Test frontend/backend/both after fix?** both
    - **Blocked on something:** None

**Upcoming and Future Tasks**
**Upcoming Tasks:**
- **P1: Slack/Teams Notifications:** Integrate with messaging platforms to send real-time reminders and updates.
- **P2: Jira/Asana Sync:** Create tasks in external project management tools based on onboarding/offboarding steps.

**Future Tasks:**
- **Evidence Policies:** Implement a policy layer on top of the evidence upload feature.
- **SSO Integration:** Add Single Sign-On capabilities (e.g., with Google/M365).
- **Provisioning Light:** Basic automated user account creation in M365/Google.
- **Advanced Monetization Tiers:** Implement the billing models described in the initial plan.

**Completed work in this session**
- **Core Application Scaffolding**: Full-stack setup with FastAPI backend and React frontend. (DONE)
- **Onboarding Module**: Feature to create and manage onboarding cases from templates. (DONE)
- **User Authentication & Roles**: JWT-based login and role-based access control. (DONE)
- **PDF Report Generation**: Server-side generation of a printable report for each case. (DONE)
- **Offboarding Module**: Cloned the onboarding logic to support employee offboarding. (DONE)
- **Evidence Upload**: Implemented file uploads for tasks requiring evidence. (DONE)
- **Audit Log**: A full audit trail of user actions. (DONE)
- **DSGVO/GDPR Compliance**: Implemented a privacy center for users to request data exports and deletions. (DONE)

**Earlier issues found/mentioned but not fixed**
None. All issues identified by the testing agent during development were resolved.

**Known issue recurrence from previous fork**
None. This is the first handover.

**Code Architecture**


**Key Technical Concepts**
- **Backend:** FastAPI, MongoDB (via  for async operations).
- **Frontend:** React, React Router, Tailwind CSS.
- **UI Components:** Shadcn/UI.
- **Authentication:** JWT (JSON Web Tokens).
- **Database:** MongoDB is used, although the user initially suggested Postgres/SQLite. The agent proceeded with the default stack.
- **PDF Generation:** 
-----

WeasyPrint could not import some external libraries. Please carefully follow the installation steps before reporting an issue:
https://doc.courtbouillon.org/weasyprint/stable/first_steps.html#installation
https://doc.courtbouillon.org/weasyprint/stable/first_steps.html#troubleshooting 

----- library for server-side HTML to PDF conversion.

**key DB schema**
The application uses MongoDB. Collections are implicitly created based on Pydantic models in :
- **users**: 
- **owner_roles**: 
- **templates**: 
- **cases**: 
- **audit_logs**: 
- **gdpr_requests**: 
- **org_settings**: 

**changes in tech stack**
- The user originally requested a Postgres (Supabase) or SQLite database. The agent implemented the application using **MongoDB**, which is the standard database in the development environment. This deviation should be noted.

**All files of reference**
*   **/app/backend/server.py**: The core of the application. Contains all backend logic, API routes, database models, and seeding logic. It has grown quite large.
*   **/app/frontend/src/App.js**: The main React component that sets up all the client-side routing.
*   **/app/frontend/src/pages/**: All created pages for different features (Dashboard, Cases, Settings, AuditLog, etc.).
*   **/app/frontend/src/context/AuthContext.js**: Manages user authentication state and JWT tokens on the frontend.
*   **/app/frontend/src/components/Layout.js**: Defines the main application layout with sidebar and header.
*   **/app/requirements.md**: A document outlining the project requirements and work completed.

**Areas that need refactoring**:
- The file  is over 1200 lines long and contains models, API routes, business logic, and database seeding. It should be broken down into a more structured package (e.g., , , , ) to improve maintainability.

**key api endpoints**
- 
-  (CRUD for onboarding/offboarding cases)
-  (CRUD for templates)
- 
- 
- 
-  (To populate initial data)

**Critical Info for New Agent**
- **Language is German:** All user-facing text and communication must be in German.
- **Monolithic Backend:** The  file is very large and should ideally be refactored for better organization.
- **Database Choice:** The app uses MongoDB, not Postgres/SQLite as the user initially suggested.
- **Testing:** The  has been used successfully after each major feature implementation. Continue this practice.

**documents created in this job**
- **/app/requirements.md**: A summary of the initial problem statement, completed tasks, and upcoming features.

**Last 10 User Messages and any pending user messages**
1.  **User**: Expand the app with an audit log and make it 100% GDPR compliant. (Handled)
2.  **Agent**: Acknowledges and starts implementing the audit log and GDPR features.
3.  ... (Agent implements and tests the features) ...
4.  **Agent**: Finishes the implementation of the Audit Log & GDPR compliance.
5.  **User**: Set up the cron job and implement the Role Change flow.
6.  **Agent**: Acknowledges the new request.
7.  **Agent**: Starts work by viewing  (2 times to see different parts of the file).
- The pending user message is the request to implement the cron job and the role change flow.

**Project Health Check:**
- **Working:** The entire application is functional. The core features of onboarding, offboarding, audit logs, and GDPR compliance have been implemented and tested.
- **Broken:** None.
- **Mocked:** None.

**3rd Party Integrations**
- **weasyprint**: Installed via 
Usage:   
  pip <command> [options]

Commands:
  install                     Install packages.
  lock                        Generate a lock file.
  download                    Download packages.
  uninstall                   Uninstall packages.
  freeze                      Output installed packages in requirements format.
  inspect                     Inspect the python environment.
  list                        List installed packages.
  show                        Show information about installed packages.
  check                       Verify installed packages have compatible dependencies.
  config                      Manage local and global configuration.
  search                      Search PyPI for packages.
  cache                       Inspect and manage pip's wheel cache.
  index                       Inspect information available from package indexes.
  wheel                       Build wheels from your requirements.
  hash                        Compute hashes of package archives.
  completion                  A helper command used for command completion.
  debug                       Show information useful for debugging.
  help                        Show help for commands.

General Options:
  -h, --help                  Show help.
  --debug                     Let unhandled exceptions propagate outside the
                              main subroutine, instead of logging them to
                              stderr.
  --isolated                  Run pip in an isolated mode, ignoring
                              environment variables and user configuration.
  --require-virtualenv        Allow pip to only run in a virtual environment;
                              exit with an error otherwise.
  --python <python>           Run pip with the specified Python interpreter.
  -v, --verbose               Give more output. Option is additive, and can be
                              used up to 3 times.
  -V, --version               Show version and exit.
  -q, --quiet                 Give less output. Option is additive, and can be
                              used up to 3 times (corresponding to WARNING,
                              ERROR, and CRITICAL logging levels).
  --log <path>                Path to a verbose appending log.
  --no-input                  Disable prompting for input.
  --keyring-provider <keyring_provider>
                              Enable the credential lookup via the keyring
                              library if user input is allowed. Specify which
                              mechanism to use [auto, disabled, import,
                              subprocess]. (default: auto)
  --proxy <proxy>             Specify a proxy in the form
                              scheme://[user:passwd@]proxy.server:port.
  --retries <retries>         Maximum attempts to establish a new HTTP
                              connection. (default: 5)
  --timeout <sec>             Set the socket timeout (default 15 seconds).
  --exists-action <action>    Default action when a path already exists:
                              (s)witch, (i)gnore, (w)ipe, (b)ackup, (a)bort.
  --trusted-host <hostname>   Mark this host or host:port pair as trusted,
                              even though it does not have valid or any HTTPS.
  --cert <path>               Path to PEM-encoded CA certificate bundle. If
                              provided, overrides the default. See 'SSL
                              Certificate Verification' in pip documentation
                              for more information.
  --client-cert <path>        Path to SSL client certificate, a single file
                              containing the private key and the certificate
                              in PEM format.
  --cache-dir <dir>           Store the cache data in <dir>.
  --no-cache-dir              Disable the cache.
  --disable-pip-version-check
                              Don't periodically check PyPI to determine
                              whether a new version of pip is available for
                              download. Implied with --no-index.
  --no-color                  Suppress colored output.
  --use-feature <feature>     Enable new functionality, that may be backward
                              incompatible.
  --use-deprecated <feature>  Enable deprecated functionality, that will be
                              removed in the future.
  --resume-retries <resume_retries>
                              Maximum attempts to resume or restart an
                              incomplete download. (default: 5) for server-side PDF generation. No external keys required.

**Testing status**
- **Testing agent used after significant changes:** YES. The agent was called three times, after the initial build, after adding the offboarding/evidence module, and after adding the audit/GDPR module.
- **Troubleshoot agent used after agent stuck in loop:** NO.
- **Test files created:** None. Testing was performed by the testing sub-agent.
- **Known regressions:** None.

**Credentials to test flow:**
The seeding process creates a default admin user.
- **Email**: 
- **Password**: </analysis>
